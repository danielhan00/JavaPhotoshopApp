package controller;

/**
 * Interface for the application controller. Implementation applies image modifications and manages
 * images in a list structure.
 */
public interface IImageController {

  /**
   * Execute the commands outlined in the implmenting classes.
   */
  void executeCommands();
}
